<!-- From: file:/C:/Users/jgillis/Downloads/Menu/app/src/main/AndroidManifest.xml --><manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="com.example.menu">

    <!--
     The action bar is included in all activities that use the 
	Theme.Holo theme (or one of its descendants), which is the default 
	theme when either the targetSdkVersion or minSdkVersion attribute 
	is set to "11" or greater.


    -->
    <uses-sdk android:minSdkVersion="11" android:targetSdkVersion="17"/>

    <!--
     The action bar can be removed by setting 
	android:theme to @android:style/Theme.Holo.NoActionBar


    -->

    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        <activity android:label="@string/app_name" android:name="com.example.menu.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:label="@string/title_activity_calculator" android:name="com.example.menu.Calculator">
        </activity>
        <activity android:label="@string/second" android:name="com.example.menu.Second">
        </activity>
        <activity android:label="@string/title_activity_menu_handler" android:name="com.example.menu.MenuHandler">
        </activity>
    </application>

</manifest>
